'use strict';

// import Movie from './animation/movie.js';
    // new Movie();
//     const target = document.getElementById('title').children[0];

// const movie = new Movie(target);

// window.addEventListener('scroll', movie.onScroll(window.scrollY));

// window.addEventListener('mousemove', e => movie.onMouseMove(e));

// target.addEventListener('mouseenter', () => {movie.onMouseEnter()});
// target.addEventListener('mouseleave', () => {movie.onMouseLeave()});
// movie.lineText.addEventListener('mouseenter', movie.onMouseEnter());